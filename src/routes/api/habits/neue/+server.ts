//neue deutsche

import { json } from '@sveltejs/kit';

export const POST = (req) => {
	//alle varten auf das licht
	/*
	request stuff:
	need name, description
	 */



	return json("lowk idk what im doing yet")
}